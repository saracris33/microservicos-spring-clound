# CONFIGURA NOME DA APLICACAO
spring:
  application:
    name: 'loja'
   
# CONFIGURA EUREKA CLIENTE
eureka:
  client:
    fetch-registry: true 
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka